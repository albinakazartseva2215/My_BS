// "Автоматическое истечение через несколько минут и удаление истекших
// удержаний" (требование 4 задачи). Физически строки не удаляются —
// docs/db-schema.md осознанно хранит их как status='expired' для истории
// (раздел "Спорные решения", п.10); "удаление" здесь — это то, что имеет
// значение для пользователя: слот перестаёт считаться занятым и
// освобождается для новой брони. Частичный уникальный индекс
// ux_appt_master_slot_active действует только на hold/confirmed, поэтому
// смены статуса достаточно.
//
// Запускается лениво (перед расчётом свободного времени, перед созданием
// удержания/записи) и дополнительно по таймеру в index.js — так истёкшие
// удержания не залёживаются, даже если никто прямо сейчас не делает запрос.

import { releaseExpiredHoldsSql } from '../db/repositories/appointments.js';
import { dateToSql } from '../time/salonClock.js';

export function releaseExpiredHolds(now = new Date()) {
  return releaseExpiredHoldsSql(dateToSql(now));
}
