-- Источник истины — docs/db-schema.md, раздел 3.1а (правки схемы сначала
-- вносятся в документ, эта миграция дословно повторяет DDL оттуда).
-- 001-004 уже применены и не редактируются — это отдельная миграция.
--
-- HOLD_DURATION_MINUTES был переменной окружения — недосмотр: это такой
-- же продуктовый параметр бронирования, как уже существующие
-- booking_step_minutes/booking_horizon_days в этой же таблице, а не
-- секрет и не инфраструктурная настройка. Администратор салона должен
-- мочь поменять его тем же способом, что и остальные параметры
-- salon_profile — без деплоя нового .env.

ALTER TABLE salon_profile
  ADD COLUMN hold_duration_minutes INTEGER NOT NULL DEFAULT 10 CHECK (hold_duration_minutes > 0);
